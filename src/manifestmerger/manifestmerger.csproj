<Project Sdk="Microsoft.Build.NoTargets">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netstandard2.0</TargetFramework>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <OutputPath Condition="'$(OutputPath)'==''">bin\$(Configuration)</OutputPath>
  </PropertyGroup>

  <ItemGroup>
    <!-- This is an ordering dependency: build java-source-utils before manifestmerger so their Gradle invocations cannot overlap. -->
    <ProjectReference Include="..\..\external\Java.Interop\tools\java-source-utils\java-source-utils.csproj" ReferenceOutputAssembly="False" />
    <ProjectReference Include="..\androidsdk\androidsdk.csproj" ReferenceOutputAssembly="False" />
  </ItemGroup>
  
  <Import Project="..\..\Configuration.props" />
  
  <Import Project="manifestmerger.targets" />
</Project>