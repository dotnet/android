#nullable enable

namespace Microsoft.Android.Sdk.TrimmableTypeMap;

// All model types (ComponentInfo, IntentFilterInfo, MetaDataInfo, PermissionInfo,
// AssemblyManifestInfo, etc.) are defined in JavaPeerInfo.cs (Scanner).
// ComponentKind enum is also defined there.
// This file is intentionally empty — kept as a placeholder in case generator-only
// model types are needed in the future.
