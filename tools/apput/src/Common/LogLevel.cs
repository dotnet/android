namespace ApplicationUtility;

/// <summary>
/// Defines the severity levels for log messages.
/// </summary>
public enum LogLevel
{
	None    = 0,
	Debug   = 1,
	Info    = 2,
	Warning = 3,
	Error   = 4,
}
